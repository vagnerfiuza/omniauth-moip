require 'spec_helper'

describe Omniauth::Moip do
  it 'has a version number' do
    expect(Omniauth::Moip::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
